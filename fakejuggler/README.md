# FakeJuggler

This is a small helper binary used for testing any sort of UI/Client talking with Juggler using HTTP API.
It's designed to run interactively and respond to commands from keyboard.

## Usage

`go run main.go`
